RuleEngine

负责：

解析规则。

CandidateEngine

负责：

生成候选字。

CombineEngine

负责：

组合名字。

PhoneticEngine

负责：

检测读音。

CourtesyEngine

负责：

生成字。

ValidatorEngine

负责：

最终验证。

RandomEngine

负责：

随机输出。

HanWeiEngine

负责：

调用全部模块。